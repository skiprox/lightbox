/**
 * ImageZoomNew mocha test spec - https://mochajs.org/
 * @param {Object} ImageZoomNew - ImageZoomNew unit tests
 */
describe('ImageZoomNew', function () {

	var imageZoomNew;

	beforeEach(function(){

	});

	it('should be a function', function () {
		assert.equal('function', typeof ImageZoomNew);
	});

});
